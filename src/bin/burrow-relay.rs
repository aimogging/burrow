//! Placeholder for the burrow-relay server binary. Real implementation
//! lands in step 3 of the WSS pivot. Kept compilable so `cargo build` and
//! `cargo test` (which compile every declared `[[bin]]`) succeed during
//! the intermediate transport-refactor step.
fn main() {
    eprintln!("burrow-relay: not yet implemented");
    std::process::exit(2);
}
